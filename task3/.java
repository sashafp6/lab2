def is_ascending(n): # Проверяет, возрастают ли цифры числа
    s = str(n) # Преобразует число в строку
    for i in range(1, len(s)): # Перебирает цифры, начиная со второй
        if s[i] <= s[i-1]: # Если текущая цифра не больше предыдущей
            return False # Не возрастает
    return True # Возрастает

def reverse_number(n): # Переворачивает число
    return int(str(n)[::-1]) # Преобразует в строку, переворачивает, обратно в число

numbers = [4, 87, 129, 33, 45] # Список чисел
print("Результат:", end=" ") # Выводит "Результат: " без переноса строки
for num in numbers: # Перебирает список чисел
    if is_ascending(num): # Если число возрастающее
        print(reverse_number(num), end=" ") # Выводит перевернутое число без переноса строки
print() # Выводит перенос строки
